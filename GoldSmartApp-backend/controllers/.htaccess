# Deny all access to controllers directory
Order deny,allow
Deny from all

# Prevent directory listing
Options -Indexes

# Block all file access
<FilesMatch ".*">
    Order deny,allow
    Deny from all
</FilesMatch>
