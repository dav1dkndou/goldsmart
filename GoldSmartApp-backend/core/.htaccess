# Deny all access to core directory
Order deny,allow
Deny from all

# Prevent directory listing
Options -Indexes

# Block all file access
<FilesMatch ".*">
    Order deny,allow
    Deny from all
</FilesMatch>
